export default function ({ route, redirect }) {
    // route.name chứa tên của route hiện tại
    // if (route.name.includes('admin') && localStorage.getItem('user') && JSON.parse(localStorage.getItem('user')).Role!=='admin') {
    //     return redirect('/');
    // }
  }